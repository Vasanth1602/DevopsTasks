# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy your static website files to Nginx's default directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 (default HTTP port)
EXPOSE 80
